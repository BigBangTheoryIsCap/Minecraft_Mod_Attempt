package net.alokyadav.tutorialmod.datagen.model_provider;

//import net.alokyadav.tutorialmod.blocks.ModBlocks
import net.minecraft.block.Block;
import net.minecraft.data.client.BlockStateModelGenerator;

                            //public class BlockSetRecipeAndModelProvider {
 //
   //                             public static void generateAllForMaterial(String materialName, BlockRegistrationPool pool, BlockStateModelGenerator modelGen) {
     //                               try {
       //                                 Class<?> blocks = ModBlocks.class;
         //
           //                             Block stairs = (Block) blocks.getField(materialName + "_STAIRS").get(null);
             //                           Block slab = (Block) blocks.getField(materialName + "_SLAB").get(null);
               //                         Block fence = (Block) blocks.getField(materialName + "_FENCE").get(null);
                               //         Block fenceGate = (Block) blocks.getField(materialName + "_FENCE_GATE").get(null);
                 //                       Block pressurePlate = (Block) blocks.getField(materialName + "_PRESSURE_PLATE").get(null);
                   //                     Block button = (Block) blocks.getField(materialName + "_BUTTON").get(null);
                     //                   Block wall = (Block) blocks.getField(materialName + "_WALL").get(null);
                       //                 Block door = (Block) blocks.getField(materialName + "_DOOR").get(null);
                         //               Block trapdoor = (Block) blocks.getField(materialName + "_TRAPDOOR").get(null);
                           //
                                        // Register with pool
                             //           pool.stairs(stairs);
                                 //       pool.slab(slab);
                                   //     pool.fence(fence);
                                     //   pool.fenceGate(fenceGate);
                                       // pool.pressurePlate(pressurePlate);
                                        //pool.button(button);
                                        //pool.wall(wall);
                            //
                               //          // Register models
                                 //       modelGen.registerDoor(door);
                                   //     modelGen.registerTrapdoor(trapdoor);

                               //     } catch (Exception e) {
                                 //       throw new RuntimeException("Failed to generate block recipes and models for material: " + materialName, e);
                                   // }
                              //  }
                            //}

